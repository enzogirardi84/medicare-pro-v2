name: nextgen-guardrails-load

on:
  workflow_dispatch:

jobs:
  guardrails-load:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    defaults:
      run:
        working-directory: nextgen_platform

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Prepare report directory
        run: mkdir -p benchmark_results

      - name: Build/start stack and wait health
        uses: ./.github/actions/nextgen-stack-up
        with:
          project_dir: nextgen_platform
          compose_file: infra/docker/docker-compose.yml
          healthcheck_url: http://localhost:8000/ready
          max_attempts: "40"
          sleep_seconds: "3"

      - name: Run k6 guardrails benchmark
        run: |
          docker run --rm --network host \
            -v "$PWD:/work/nextgen_platform" \
            -w /work/nextgen_platform \
            grafana/k6 run tests/load/k6_guardrails_resilience.js \
            --env BASE_URL=http://localhost:8000 \
            --summary-export benchmark_results/guardrails-summary.json

      - name: Summarize benchmark
        run: python scripts/summarize_benchmark.py benchmark_results/guardrails-summary.json | tee benchmark_results/guardrails-report.txt

      - name: Upload benchmark artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nextgen-guardrails-load
          path: |
            nextgen_platform/benchmark_results/guardrails-summary.json
            nextgen_platform/benchmark_results/guardrails-report.txt

      - name: Shutdown stack
        if: always()
        uses: ./.github/actions/nextgen-stack-down
        with:
          project_dir: nextgen_platform
          compose_file: infra/docker/docker-compose.yml
